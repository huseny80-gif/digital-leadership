/**
 * Subjects listing (structural placeholder). Will consume
 * GET /api/v1/subjects (see API_ARCHITECTURE.md) once Phase 7 implements it.
 */
export default function SubjectsPage() {
  return (
    <section>
      <h1>Subjects</h1>
      <p>Structural placeholder.</p>
    </section>
  );
}
