/**
 * User dashboard (structural placeholder). Business content — recent
 * subjects/lectures, quiz results, etc. — is implemented in Phase 8.
 */
export default function DashboardPage() {
  return (
    <section>
      <h1>Dashboard</h1>
      <p>Structural placeholder.</p>
    </section>
  );
}
