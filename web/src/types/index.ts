/**
 * Web-only view-model types belong here (e.g., derived/UI-specific shapes).
 * Types that describe the API contract itself belong in the `shared`
 * package (`@shared/*`) so the backend and both clients agree on one
 * definition — do not duplicate those here.
 */
export {};
